{
  "name": "codex-gateway",
  "version": "0.5.1",
  "private": true,
  "description": "Minimal multi-session HTTP/SSE gateway for codex app-server, now implemented in Rust.",
  "scripts": {
    "start": "cargo run --bin codex-gateway",
    "web": "cargo run --bin codex-gateway",
    "cli": "cargo run --bin codex-gateway-cli --",
    "check": "cargo check"
  }
}
